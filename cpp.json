{
  "enum class": {
		"prefix": "enumc",
		"body": [
			"enum class ${1:Name} {",
      "  $2",
      "};"
		],
		"description": "Code snippet for enum class"
	},
  "for loop": {
		"prefix": "for",
		"body": [
			"for (${size_t} ${i} = ${1:0}; ${i} < ${2:count}; ++${i}) {",
			"  $3",
			"}"
		],
		"description": "Code snippet for 'for' loop"
	},
  "for-each loop": {
		"prefix": "foreach",
		"body": [
			"for(auto ${1:var} : ${2:iterable}) {",
			"  $3",
			"}"
		],
		"description": "Code snippet for range-based for loop"
	},
  "WinMain function": {
    "prefix": "winmain",
    "body": [
      "int __stdcall wWinMain(HINSTANCE instance, HINSTANCE previousInstance, LPWSTR cmdLine, int showCode) {",
      "  return 0;",
      "}"
    ],
    "description": "Code snippet for WinMain function"
  }
}